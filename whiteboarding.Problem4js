// Problem
// Write a function that counts the number of vowels in a string. The vowels are "a", "e", "i", "o" & "u"
// findVowels('hello') // --> 2
// findVowels('why') // --> 0

// Problem Breakdown
// have the vowels as something we can compare the string parameter to
// determine if each individual characters is a vowels or not
// count vowels
// collect the count into something that we can print/log.

// Pseudocode
// 1. Make a list of vowel characters.
// 2. Loop through the characters of the vowels
//      And compare each character to see if it fits something
//      includes vowels.
// 3. If the comparison is truly a vowel, then add one to the count of vowels.
// 4. Print the final count of vowels after every character has been compared.

// Methods we might use
// foreach()
// .every()
// filter()
//.includes(value toFind);

function findVowelCount(inputString) {
//   const vowels = ["a", "e", "i", "o", "u"];
//   let vowelCount = 0;
//   vowels.forEach(vowel => {
//     if (inputString.toLowerCase().includes(vowel)) {
//       vowelCount += 1;
//     }
//   });
//   console.log(vowelCount);
// }
findVowelCount("Test String"); // expected output is 2.


